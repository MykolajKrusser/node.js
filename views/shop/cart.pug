extends ../layouts/main-layout.pug

block content
  main.main_content
    if products.length > 0
      ul
        each product in products
          li #{product.productData.title} (#{product.qty})
    else
      h1 No products in cart!
